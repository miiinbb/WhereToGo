# AI QA Scenarios

이 문서는 최종 동작 기준의 확인용 시나리오입니다.
검증 대상은 `app.js`, `server.js`, `index.html`, `export.js`, `style.css`, `package.json`이며, 오래된 문서는 기준으로 삼지 않습니다.

## 공통 확인 기준

- 여행 옵션은 3개가 생성되어야 합니다.
- 상세 일정은 선택된 옵션 수와 일수 기준을 최대한 따라야 합니다.
- 일정 수정은 기존 계획을 유지한 채 수정 내용을 반영해야 합니다.
- API Key가 없으면 명확한 안내가 보여야 합니다.
- 오류가 나더라도 화면이 깨지지 않고 원인이 보이는 메시지가 나와야 합니다.
- API 요청은 서버 경로(`/api/travel-options`, `/api/travel-details`, `/api/travel-revise`)를 사용해야 합니다.

## 1. 초기 진입 확인

- `index.html`을 연다.
- 화면에 메인 UI가 보이는지 확인한다.
- `app.js`와 `export.js`가 함께 동작하는지 확인한다.

기대 결과:

- 앱이 정상적으로 렌더링된다.
- 상단에 AI 상태 배지가 보인다.
- AI 설정 화면으로 이동할 수 있다.

## 2. Gemini 설정 저장

- AI 설정 화면을 연다.
- Provider를 `Gemini`로 선택한다.
- Gemini API Key를 입력한다.
- 필요하면 모델을 `gemini-2.5-flash`로 둔다.
- 저장한다.
- 브라우저를 새로고침한다.

기대 결과:

- 설정이 유지된다.
- AI 상태 배지에 Gemini와 모델명이 보인다.
- `sessionStorage`에 AI 설정이 남는다.

## 3. Groq 설정 저장

- AI 설정 화면을 연다.
- Provider를 `Groq`로 선택한다.
- Groq API Key를 입력한다.
- 모델을 `llama-3.3-70b-versatile`로 둔다.
- 저장한다.
- 브라우저를 새로고침한다.

기대 결과:

- 설정이 유지된다.
- AI 상태 배지에 Groq와 모델명이 보인다.
- 저장 후에도 동일 provider/model 조합이 유지된다.

## 4. Gemini 옵션 생성

- Gemini API Key를 저장한 상태로 여행 조건을 입력한다.
- `옵션 보기`를 누른다.

기대 결과:

- 요청이 `/api/travel-options`로 전송된다.
- 응답이 3개 옵션으로 표시된다.
- 옵션 카드와 비교표가 정상 렌더링된다.
- 옵션이 3개 미만이면 fallback이 작동해 3개가 유지된다.

## 5. Groq 옵션 생성

- Groq API Key를 저장한 상태로 같은 조건을 입력한다.
- `옵션 보기`를 누른다.

기대 결과:

- 요청이 `/api/travel-options`로 전송된다.
- 3개 옵션이 표시된다.
- provider가 바뀌어도 흐름이 동일하게 동작한다.

## 6. 상세 일정 생성

- 옵션 3개를 생성한다.
- 옵션을 선택한 뒤 `상세 일정`을 누른다.

기대 결과:

- 요청이 `/api/travel-details`로 전송된다.
- 선택된 옵션 수만큼 일정이 생성된다.
- itinerary가 부족하면 fallback 일정이 보정된다.
- 상세 카드가 깨지지 않는다.

## 7. 일정 수정

- 상세 일정 화면에서 수정 문장을 입력한다.
- 예: `아침 시작을 늦추고 이동을 줄여줘`
- `수정 반영`을 누른다.

기대 결과:

- 요청이 `/api/travel-revise`로 전송된다.
- 수정 요약이 보인다.
- 수정 후에도 계획 카드가 유지된다.
- 응답이 불완전해도 기존 일정을 유지하는 fallback이 작동한다.

## 8. API Key 없음

- AI 설정에서 API Key를 비운다.
- 저장한다.
- 옵션 생성이나 상세 일정 생성 버튼을 누른다.

기대 결과:

- `API Key 필요` 또는 이에 준하는 명확한 메시지가 표시된다.
- 화면이 멈추지 않는다.
- 서버 호출이 실패하면 원인이 보인다.

## 9. 잘못된 모델명

- Provider는 유지하고 모델명을 일부러 틀리게 입력한다.
- 요청을 실행한다.

기대 결과:

- 모델 오류가 구분된 메시지로 보인다.
- 네트워크 오류나 인증 오류와 구분된다.

## 10. JSON 응답 파싱 복구

- 가능한 경우 코드블록 형태나 약간 불완전한 JSON 응답을 반환하도록 테스트한다.
- 옵션 생성, 상세 일정, 수정 모두 확인한다.

기대 결과:

- JSON 파싱 복구가 시도된다.
- 완전히 복구 불가하면 파싱 실패 메시지가 표시된다.

## 11. 서버 연결 확인

- 브라우저 개발자 도구에서 네트워크를 본다.
- 옵션 생성, 상세 일정 생성, 수정 요청을 각각 실행한다.

기대 결과:

- 실제 요청 대상이 `http://localhost:3000/api/...`인지 확인된다.
- UI에서 저장한 AI 설정이 요청 본문에 포함된다.

## 12. mock 모드 확인

- 코드에서 `CONFIG.USE_MOCK = true`로 바꿔 테스트한다.

기대 결과:

- 실제 AI 호출 없이도 화면이 끝까지 동작한다.
- 옵션과 일정 생성이 정상적으로 이어진다.

## 최종 통과 기준

- Gemini와 Groq 둘 다 옵션 생성이 된다.
- 상세 일정이 생성된다.
- 일정 수정이 된다.
- API Key가 없을 때 안내가 명확하다.
- 어떤 경로가 사용되는지 코드와 네트워크에서 확인된다.
